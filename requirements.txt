# NavSense shared requirements
# Install this first on every platform:
#   python -m pip install -r requirements.txt

# Core computer vision and app runtime
opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0
scipy>=1.10.0
pillow>=10.0.0
pyyaml>=6.0

# Voice / app logic
SpeechRecognition>=3.10.0
requests>=2.31.0
python-dotenv>=1.0.1

# TTS / media
pyttsx3>=2.90
pygame>=2.6.0
gTTS>=2.5.1

# Raspberry Pi sensor helpers are safe cross-platform,
# but only become active on Pi when configured.
gpiozero>=2.0.1
smbus2>=0.4.3
